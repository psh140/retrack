# 배포 체크리스트 (AWS EC2)

프론트엔드 UI 일관성 작업(2026-07-24, 커밋 `e23b79f`)까지 완료한 시점 기준으로,
운영 배포까지 남은 작업을 정리한 문서다.

관련 문서: [`nginx-배포설정.md`](nginx-배포설정.md) — Nginx 리버스 프록시·SPA 폴백 설정

---

## 1. 코드 준비 (배포 전 필수)

### 1-1. 시크릿 분리 — 완료 (2026-07-27)

저장소가 **공개 상태**임을 확인했다(GitHub API 200). 커밋된 평문 자격증명은 이미 외부에 노출된 것으로
간주하고, 코드에서 지우는 것에 그치지 않고 **값 자체를 폐기·재발급**했다.

| 위치                    | 조치                                                 | 상태 |
| ----------------------- | ---------------------------------------------------- | ---- |
| `docker-compose.yml:8`  | `POSTGRES_PASSWORD` → `${DB_PASSWORD}`               | [x]  |
| `docker-compose.yml:31` | `DB_PASSWORD` → `${DB_PASSWORD}`                     | [x]  |
| `docker-compose.yml:32` | `JWT_SECRET` → `${JWT_SECRET}`                       | [x]  |
| `spring-db.xml:9-19`    | 하드코딩된 jdbcUrl·username·password → 환경변수 참조 | [x]  |
| `.env` / `.env.example` | `DB_PASSWORD`, `JWT_SECRET` 키 추가                  | [x]  |
| `CLAUDE.md:130`         | DB 접속 정보의 평문 비밀번호 제거                    | [x]  |

**주의 — `spring-db.xml` 하드코딩 문제**

`docker-compose.yml`이 `DB_HOST`·`DB_PORT`·`DB_NAME`·`DB_USER`·`DB_PASSWORD`를 넘기고 있었지만,
백엔드는 이 값들을 **전혀 읽지 않고** `spring-db.xml`에 직접 적힌 접속 정보를 쓰고 있었다.
compose만 `${DB_PASSWORD}`로 바꿨다면 DB 접속이 그대로 옛 비밀번호를 향해 실패했을 것이다.
`spring-mvc.xml:96`의 기존 패턴(`#{systemEnvironment['...']}`)에 맞춰 교체했다.

`JWT_SECRET`은 `JwtUtil.java:34`에서 `System.getenv()`로 정상적으로 읽고 있어 별도 수정이 없었다.

**변경 후 필수 절차**

1. WAR 재빌드 — `spring-db.xml`은 WAR에 포함되므로 재빌드해야 반영된다
2. 실제 PostgreSQL 계정 비밀번호 변경 — `POSTGRES_PASSWORD`는 볼륨 최초 생성 시에만 적용되므로,
   기존 볼륨에서는 `ALTER USER`로 직접 바꿔야 한다
   ```bash
   docker exec -it retrack-db psql -U retrack -d retrack \
     -c "ALTER USER retrack WITH PASSWORD '<새 비밀번호>';"
   ```
3. JWT 서명키 교체로 기존 발급 토큰은 모두 무효 — 재로그인 필요

**git 히스토리**: 재작성하지 않기로 결정했다. 노출된 값을 폐기한 이상 히스토리에 남은 옛 값은
무의미하고, GitHub 캐시·기존 클론본은 어차피 회수가 불가능하다.

**EC2 배포 시**: 로컬 `.env` 값을 재사용하지 말고 서버에서 새로 생성한다.

### 1-2. 운영용 compose 오버라이드 — 완료 (2026-07-29)

`docker-compose.prod.yml`을 작성했다. 로컬 개발 환경(`docker-compose.yml`)은 그대로 두고
EC2에서만 오버라이드를 적용한다.

```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

| 대상               | 개발                    | 운영                                           | 상태 |
| ------------------ | ----------------------- | ---------------------------------------------- | ---- |
| seed.sql 마운트    | 테스트 데이터 자동 주입 | 제거                                           | [x]  |
| DB `5432:5432`     | 호스트에 노출           | 노출 제거 — 백엔드가 같은 도커 네트워크로 접근 | [x]  |
| 백엔드 `8080:8080` | 호스트에 노출           | 노출 제거 — Nginx가 `/api`를 프록시            | [x]  |
| 프론트엔드 포트    | `3000:80`               | `80:80` — 표준 HTTP 포트                       | [x]  |
| 재시작 정책        | 없음                    | `restart: unless-stopped` (3개 서비스)         | [x]  |

**주의 — Compose 버전**

포트 노출 제거와 seed.sql 제외에 `!reset` / `!override` 태그를 썼다.
이 태그는 **Docker Compose v2.24.0 이상**에서만 동작하므로, EC2 세팅 후 버전을 확인해야 한다.

```bash
docker compose version
```

**검증 결과** (로컬, 2026-07-29)

```bash
# 운영 오버라이드 적용 시 — db·backend 포트 노출 없음, seed.sql 없음, frontend 80:80
docker compose -f docker-compose.yml -f docker-compose.prod.yml config

# 개발 단독 실행 시 — 기존과 동일 (5432 / 8080 / 3000 노출, seed.sql 유지)
docker compose -f docker-compose.yml config
```

### 1-3. HTTPS용 nginx 설정 — 완료 (2026-08-03)

**`frontend/nginx.conf`는 그대로 두고 `frontend/nginx.prod.conf`를 새로 만들었다.**
443 블록은 인증서 파일이 실재해야 nginx가 기동하므로, 한 파일에 합치면 인증서가 없는
로컬 개발 환경에서 프론트엔드 컨테이너가 뜨지 않기 때문이다.

| 환경 | 사용 설정 | 적용 방식 |
|---|---|---|
| 로컬 개발 | `nginx.conf` (80만) | Dockerfile이 이미지에 COPY |
| 운영(EC2) | `nginx.prod.conf` (80 리다이렉트 + 443) | `prod.yml`에서 볼륨 마운트로 덮어쓰기 |

- [x] 443 서버 블록 + 인증서 경로 추가
- [x] HTTP → HTTPS 리다이렉트 추가
- [x] `server_name _;` → `hughpark.com www.hughpark.com`
- [x] `proxy_set_header Origin "";` — HTTPS 전환 후 로그인이 403으로 막히던 문제 해결
      (상세: [`배포-작업기록.md`](배포-작업기록.md) 8-1)

볼륨 마운트 방식이라 **설정만 바꿀 때는 이미지 재빌드 없이** 파일 교체 후
`docker exec retrack-frontend nginx -s reload`로 반영된다.

---

## 2. 직접 처리해야 하는 항목 (AWS 콘솔)

2026-08-03 기준 이 섹션은 **전 항목 완료**했다.

- [x] **도메인 구매** — `hughpark.com`. Route 53에서 직접 구매해 호스팅 영역이 자동 생성되었고,
      네임서버 위임 작업이 필요 없었다. → 상세 절차: [`도메인-구매-가이드.md`](도메인-구매-가이드.md)
- [x] **EC2 인스턴스 생성** — Ubuntu 24.04 LTS, t3.small (월 $15 내외), 인스턴스 ID `i-0b83878085e74f5c6`
      - 2026-08-03 변경: 모든 서비스가 도커 컨테이너 안에서
      동작해 호스트 OS 버전 의존이 없고, 24.04가 지원 기간도 길어 24.04로 변경했다.
      26.04는 도커 공식 저장소 대응이 불확실해 선택하지 않았다. - 스토리지 20GB gp3 (기본 8GB는 도커 이미지로 부족)
      - 고급 세부 정보: 종료 방지 활성화, 크레딧 사양 '무제한' 해제(표준) — 크레딧 소진 시
      추가 과금 대신 성능 저하를 택했다
- [x] **키페어 발급** 및 SSH 접속 확인 — `retrack-key.pem`. 저장소에 두지 않는다(`.gitignore`에 `*.pem` 등록).
      키 파일 권한을 좁히지 않으면 SSH가 접속을 거부한다
      - Windows: `icacls <키 경로> /inheritance:r /grant:r "%USERNAME%:R"`
      - macOS: `chmod 400 ~/.ssh/retrack-key.pem`
      - 접속: `ssh -i <키 경로> ubuntu@52.78.210.188`
- [x] **Elastic IP 할당** — `52.78.210.188` (ap-northeast-2). 인스턴스 연결까지 완료.
      할당만 하고 미연결로 두면 시간당 요금이 부과된다
- [x] **보안 그룹 인바운드** — 22(SSH), 80(HTTP), 443(HTTPS)만 개방. 5432·8080은 열지 않는다
      - 80은 Certbot HTTP-01 챌린지와 HTTP→HTTPS 리다이렉트에 필요하므로 생략하면 안 된다
      - SSH 소스는 `0.0.0.0/0` 유지 — 개발 환경이 Windows·macOS 두 곳이라 `내 IP` 고정이 비현실적이다.
      Ubuntu AMI는 비밀번호 인증이 꺼져 있어 키 없이는 접속되지 않는다
- [x] **Route 53 A 레코드** — `hughpark.com`, `www.hughpark.com` 둘 다 → `52.78.210.188` (TTL 300).
      외부 DNS(8.8.8.8) 전파 확인 완료. NS·SOA는 자동 생성분이므로 수정하지 않는다

---

## 3. 서버 세팅 절차 — 완료 (2026-08-03)

> 실제 수행한 명령과 각 단계의 근거는 [`배포-작업기록.md`](배포-작업기록.md) 3~5장에 정리했다.

0. [x] **swap 2GB 생성** — 계획에 없던 선행 작업. t3.small의 RAM 2GB로는 Vite 빌드가 OOM으로 죽는다.
       `/etc/fstab`에 등록해 재부팅 후에도 유지시킨다
1. [x] EC2 접속 후 Docker, Docker Compose 설치 — 배포판 기본 패키지 대신 **Docker 공식 저장소** 사용
2. [x] GitHub 저장소 clone — 서버에는 GitHub SSH 키가 없으므로 **HTTPS 주소**로 받는다
3. [x] `.env` 작성 — `DB_PASSWORD`, `JWT_SECRET`은 서버에서 `openssl rand`로 새로 생성.
       `MAIL_PASSWORD`는 구글 계정 비밀번호가 아니라 **Gmail 앱 비밀번호**(16자)다
4. [x] 백엔드 WAR 빌드 후 업로드
   - 로컬 IntelliJ에서 Maven package → `backend/target/retrack-backend-1.0-SNAPSHOT.war`
   - `scp`로 서버의 동일 경로에 배치 (compose가 이 경로를 마운트한다)
   - **`mkdir -p backend/target`을 먼저 해야 한다.** 디렉터리가 없으면 Docker가 마운트 경로를
     빈 디렉터리로 만들어버려 Tomcat이 WAR를 배포하지 못한다
5. [x] `docker compose version` 확인 — `!reset`/`!override` 사용을 위해 v2.24.0 이상 필요
       (실제 설치본: Docker 29.7.1 / Compose v5.3.1)
6. [x] `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d` 실행
7. [x] 컨테이너 3개(db / backend / frontend) 기동 확인 —
       **backend·db에 호스트 포트가 없고 frontend만 열려 있어야** 오버라이드가 제대로 먹은 것이다

### 참고 — 백엔드 재배포 시 Nginx 502 (해결 완료, 2026-07-29)

과거에는 백엔드 컨테이너를 재생성할 때마다 `/api` 요청이 전부 502가 되어
`docker compose restart frontend`가 필요했다. Nginx가 기동 시점에 조회한 IP를
계속 재사용하는데 컨테이너 IP는 재생성 시 바뀌기 때문이다.

`frontend/nginx.conf`에 Docker 내장 DNS resolver를 명시하고 프록시 대상을 변수로 지정해
매 요청마다 재조회하도록 수정했다. **이제 백엔드 재배포 후 frontend 재시작이 불필요하다.**

```nginx
resolver 127.0.0.11 valid=10s ipv6=off;

location /api {
    set $backend_host backend;
    proxy_pass http://$backend_host:8080$request_uri;
    ...
}
```

→ 상세: [`troubleshooting-nginx-업스트림-DNS캐싱.md`](troubleshooting-nginx-업스트림-DNS캐싱.md)

---

## 4. HTTPS 발급 — 완료 (2026-08-03)

- [x] Certbot 설치(snap) 및 인증서 발급 — **standalone** 방식.
      certbot이 80 포트를 직접 잡으므로 발급 중에는 frontend를 멈춘다
- [x] 발급된 인증서를 frontend 컨테이너에 마운트 (`/etc/letsencrypt:ro`)
- [x] 자동 갱신 확인 (`certbot renew --dry-run`) — 시뮬레이션 성공
- [x] **갱신 훅 설정** — 계획에 없던 필수 작업.
      자동 갱신도 standalone으로 동작하는데 frontend가 80을 점유하고 있으면 실패한다.
      그대로 두면 90일 뒤 인증서가 만료된다.
      `/etc/letsencrypt/renewal-hooks/{pre,post}/`에 컨테이너 stop/start 스크립트를 배치했다

인증서 만료일: **2026-11-01** (90일 주기 자동 갱신)

---

## 5. 배포 후 동작 확인 — 완료 (2026-08-03)

- [x] 로그인 → 대시보드 진입
- [x] 과제 목록/상세 조회, 상태 변경
- [x] **새로고침 시 404가 나지 않는지** — SPA 폴백 확인
- [x] 파일 업로드 후 컨테이너 재시작 → 파일 유지 확인 (`./uploads` 볼륨)
- [x] 이메일 발송 확인 (수신 주소: `sangh_park@kdischool.ac.kr`)
- [x] HTTP 접속 시 HTTPS로 리다이렉트되는지

**운영 DB에는 계정이 없다.** `prod.yml`이 `seed.sql`을 제외하므로 회원가입 후
DB에서 직접 ADMIN으로 승격해야 한다. 승격 후에는 재로그인해야 새 권한이 토큰에 반영된다.

```bash
docker exec -it retrack-db psql -U retrack -d retrack \
  -c "UPDATE users SET role='ADMIN' WHERE user_id=1;"
```

**HTTPS 전환 직후 로그인이 403으로 막히는 문제**가 발생했다. 원인과 해결은
[`배포-작업기록.md`](배포-작업기록.md) 8-1 참조.

---

## 참고: 이미 완료된 항목

- [x] `frontend/Dockerfile` 멀티 스테이지 빌드 (node 빌드 → nginx 서빙) — 2026-05-26
- [x] `frontend/nginx.conf` 작성 (SPA 폴백, `/api` 리버스 프록시, `server_name _;`) — 2026-05-26
- [x] `frontend/.dockerignore` — 2026-05-26
- [x] `docker-compose.yml` frontend 포트 `3000:80` 변경 — 2026-05-26
- [x] `docs/nginx-배포설정.md` 작성 — 2026-05-26
- [x] `frontend/nginx.conf` 업스트림 DNS 재조회 (backend 재생성 시 502 해소) — 2026-07-29
- [x] `docker-compose.prod.yml` 운영 오버라이드 작성 — 2026-07-29
