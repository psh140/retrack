<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd
           http://www.springframework.org/schema/tx
           http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- 컴포넌트 스캔 - com.retrack 패키지 자동 인식 -->
    <context:component-scan base-package="com.retrack"/>

    <!-- CORS 설정 -->
    <mvc:cors>
        <mvc:mapping path="/api/**"
                     allowed-origins="http://localhost:3000"
                     allowed-methods="GET,POST,PUT,PATCH,DELETE,OPTIONS"
                     allowed-headers="*"
                     allow-credentials="true"/>
    </mvc:cors>

    <!-- JWT 인터셉터 등록 (/api/auth/** 제외, 나머지 /api/** 적용) -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/api/**"/>
            <mvc:exclude-mapping path="/api/auth/**"/>
            <ref bean="jwtInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!--
        Jackson2ObjectMapperFactoryBean — JavaTimeModule 등록으로 LocalDateTime 직렬화 지원
        @JsonFormat 어노테이션이 적용되려면 JavaTimeModule이 반드시 등록되어야 함
    -->
    <bean id="objectMapper" class="org.springframework.http.converter.json.Jackson2ObjectMapperFactoryBean">
        <property name="modulesToInstall">
            <array>
                <value>com.fasterxml.jackson.datatype.jsr310.JavaTimeModule</value>
            </array>
        </property>
    </bean>

    <!--
        트랜잭션 매니저 — Service 빈이 서블릿 컨텍스트에 있으므로 여기서 선언
        dataSource 빈은 루트 컨텍스트(spring-db.xml)에 있으며 자식 컨텍스트에서 참조 가능
    -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!-- @Transactional 어노테이션 활성화 -->
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- Spring MVC 어노테이션 활성화 + Jackson JSON UTF-8 인코딩 설정 -->
    <mvc:annotation-driven>
        <mvc:message-converters>
            <bean class="org.springframework.http.converter.StringHttpMessageConverter">
                <property name="defaultCharset" value="UTF-8"/>
            </bean>
            <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
                <property name="defaultCharset" value="UTF-8"/>
                <property name="objectMapper" ref="objectMapper"/>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

</beans>